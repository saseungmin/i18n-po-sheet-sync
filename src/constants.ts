import type { HeaderMapping } from './types';

export const DEFAULT_HEADER_MAPPING: HeaderMapping = {
  msgid: 'messageId',
  msgctxt: 'context',
  references: 'references',
  comments: 'comments',
  extractedComments: 'extractedComments',
};
